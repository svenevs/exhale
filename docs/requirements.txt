bs4==0.0.1     # BeautifulSoup!
lxml==4.1.1    # We need the lxml backend for BeautifulSoup.
sphinx==1.6.6  # Exhale is a Sphinx extension.
breathe==4.7.3 # The directives used for documentation come from the excellent Breathe.
six==1.11.0     # Primarily for Unicode string types
